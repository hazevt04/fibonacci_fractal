#ifndef __UTIL_H__
#define __UTIL_H__

/*
#define VERBOSE_PRINTF( fmt, ... )        \
   {                                      \
         if ( verbose_flag ) {            \
            printf( fmt, ##__VA_ARGS__ ); \
      }                                   \
   }


#define CHECK_NULL_PTR( ptr )                                 \
   {                                                          \
         if ( !ptr ) {                                        \
            std::cerr <<                                  \
                     "File << #ptr << " Line "%d in %s(): ERROR: " #ptr \
                     " is NULL! malloc failed?\n",            \
                     __FILE__, __LINE__, __func__ );          \
            exit( EXIT_FAILURE );                             \
      }                                                       \
   }


*/

#endif
